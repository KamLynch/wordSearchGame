words = ['organ', 'valve', 'heart', 'scapel', 'podiatrist', 'health', 'gloves', 'pulse', 'discharge']

let randomIndex = Math.floor(Math.random() * words.length); 
let selectedWord = words[randomIndex]; 
console.log(selectedWord);